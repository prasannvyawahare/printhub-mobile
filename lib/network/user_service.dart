import 'api_client.dart';

class UserService {
  final ApiClient api;

  UserService(this.api);

  // Future<List<User>> fetchUsers() => api.getUsers();
  //
  // Future<User> addUser(User user) => api.createUser(user);
}
